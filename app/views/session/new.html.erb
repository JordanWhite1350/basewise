<% content_for :title, "Sign in to Basewise" %>

<%= simple_form_for @user, url: {action: "create"} do |f| %>
  <%= f.input :email, placeholder: "Email address", error_prefix: "Email" %>
  <%= f.input :password, placeholder: "Password", error_prefix: "Password" %>

  <%= f.submit %>
<% end %>
